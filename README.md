# dotfiles
setting up dotfiles on multiple vagrant machines is so last year.

# light the fires and kick the tires

``` shell
# Fetch the submodules
git clone --recursive git@github.com:clintonhalpin/dotfiles.git ~/ 
git submodule update --init
```